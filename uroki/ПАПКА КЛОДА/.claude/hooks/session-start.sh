#!/usr/bin/env bash
# Учебный пример хука SessionStart.
# Хук — это shell-команда, которую harness Claude Code запускает САМ при наступлении
# события (SessionStart, PreToolUse, PostToolUse, Stop и т.д.). Это делает не модель,
# а сам инструмент — поэтому хуки годятся для гарантированных, повторяющихся действий.
#
# Этот хук печатает напоминание о правилах проекта в начале каждой сессии.
# Текст, выведенный в stdout, попадёт в контекст ассистента как additionalContext.

cat <<'EOF'
[Учебный проект .claude] Памятка на старте сессии:
- Это демонстрационная структура папки .claude. Объяснить её можно командой /explain-claude.
- Любой UI/сайт проектируется СНАЧАЛА как прототип в Pencil — команда /prototype-first.
- Описание структуры и установки MCP — в index.html и CLAUDE.md.
EOF
