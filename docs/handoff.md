# Handoff

## Снимок передачи

- **Дата:** 2026-08-19
- **Проект:** vibe-portal (`/opt/vibe-portal`)
- **Кто передаёт:** не указано
- **Состояние репо:** чистое рабочее дерево — снимок сделан командой `/end`
- **Что не решено:**
  - Диск хоста (24G, единственный раздел) — тесное место в принципе. Сессия
    2026-08-11 дважды роняла его в 0 свободного при пересборке образа;
    2026-08-19 потребовалась ещё одна ручная чистка (некритичная, 77%→76%).
    Структурно проблема не устранена — расширение раздела или регулярная
    профилактическая чистка (таймер) так и не заведены
  - `website-customizer` (отдельный проект/git, `/home/my_workspace` admin'а)
    — его собственные `docs/state.md`/`docs/last-session.md` всё ещё
    описывают Chromium-блокер как открытый, хотя платформенно он уже решён
    этой сессией (образ пересобран, `vibe-krutko77` пересоздан)
  - Если Chromium-зависимые скиллы понадобятся другим ученикам — их
    контейнеры тоже нужно пересоздать (`docker rm -f vibe-<user>`), это не
    сделано автоматически ни для кого, кроме `vibe-krutko77`
  - Агент в `es-trans_repairs` ещё не вызывал реальный `POST /deploy` через
    `deploy-service` — сервис готов и протестирован, но живой редеплой этого
    конкретного проекта через него ещё не выполнялся (тянется с 2026-07-24)
  - `es-trans-standard` и `a1track` тоже уже в реестре deploy-service, но
    ни разу реально не деплоились через него (только smoke-тест на
    отдельном одноразовом проекте)
  - `es-trans-test`/`test-1.es-trans.ru` (папка `es-trans-test-OM`) осознанно
    НЕ зарегистрирован в deploy-service — решение отложено на пользователя
  - `scripts/create-admin.sh` рассинхронизирован с актуальным
    `students.js → dockerCreate()` (старые лимиты ресурсов, устаревший путь
    шаблонов, нет общих volume'ов/SSH-маунтов) — воркэраунд задокументирован
    в `docs/expand_vibe-portal.md`, сам скрипт не исправлен (тянется с
    2026-07-22)
  - HTTPS на `201.51.4.183` включён и проверен curl'ом, но не проверен
    вживую в браузере под admin-сессией (тянется с 2026-07-22)
- **С чего начать:** `docs/last-session.md` → раздел «Что делать следующему»
