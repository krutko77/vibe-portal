# Handoff

## Снимок передачи

- **Дата:** 2026-09-22
- **Проект:** vibe-portal (`/opt/vibe-portal`)
- **Кто передаёт:** не указано
- **Состояние репо:** на начало сессии — 1 незакоммиченный файл
  (`deploy-service/server.js`, фикс `.env`-excludes, применён кем-то раньше,
  до этой сессии); эта сессия правок кода в самом репо не вносила (только
  диагностика + правки вне репо, см. ниже) + обновление `docs/*`. Снимок
  командой `/end` коммитит и то, и другое; **после снимка не забыть `git
  push origin master` руками** — автопуша на этом хосте нет (см. CLAUDE.md
  § «Бэкап»)
- **Что не решено:**
  - **test-2.es-trans.ru — финальное подтверждение не получено.** Обе
    найденные причины (см. `docs/last-session.md` 2026-09-22) исправлены и
    проверены покомпонентно (env-переменные в живом процессе, каталог
    `data/results` восстановлен и защищён `mkdir`-гвардом, `exclude` в
    `registry.json`, pm2-процесс online, 200 локально/через nginx), но
    живой E2E с реальной отправкой письма сознательно не запускался —
    нужно попросить автора тикета пройти тест ещё раз и подтвердить, что
    письмо дошло
  - **Не проверено, есть ли та же уязвимость у других `type:"node"`-записей
    `registry.json`** (`es-trans-orders`, `es-trans-repairs`,
    `es-trans-results`, `krutko77-test-transport-logistics`,
    `krutko77-es-trans-test-om`) — гитигноренные рантайм-папки, которые
    `rsync --delete` может стереть при следующем их редеплое так же тихо,
    как это случилось у `es-trans-standard` 22.09
  - Правки этой сессии сделаны вне git-репозитория vibe-portal:
    `/etc/vibe-deploy/registry.json` (добавлен `exclude`),
    `/opt/es-trans-standard/data/results/` (восстановлен каталог),
    `results.js` в воркспейсе `krutko77` и в прод-копии `/opt/es-trans-standard`
    (добавлен `mkdir`-гвард) — ни один из этих путей не под git этого репо,
    в `docs/*` они задокументированы вручную
  - (Из прошлой сессии, не проверено вживую) Группировка проектов по
    разделам сделана и проверена серверно/синтаксически, но
    клика-проверки в браузере пользователем ещё не было
  - Внешний баг расширения Microsoft Remote-SSH (версия 0.126.0, github.com/
    microsoft/vscode issue #329735) ломает десктопное SSH-подключение к
    контейнерам учеников — **не баг vibe-portal**, но нужно быть готовым к
    повтору у других учеников. У krutko77 обход применён (расширение
    закреплено на 0.124.0, автообновление выключено), но сам баг у Microsoft
    ещё не пофикшен по-настоящему (закрывался 25.08, вернулся 26.08). Следить
    за апстримом, см. `docs/state.md` «Следующие шаги»
  - Диск хоста (24G, единственный раздел) — тесное место в принципе. Сессия
    2026-08-11 дважды роняла его в 0 свободного при пересборке образа;
    2026-08-19 потребовалась ещё одна ручная чистка (некритичная, 77%→76%).
    Структурно проблема не устранена — расширение раздела или регулярная
    профилактическая чистка (таймер) так и не заведены
  - `website-customizer` (отдельный проект/git, `/home/my_workspace` admin'а)
    — его собственные `docs/state.md`/`docs/last-session.md` всё ещё
    описывают Chromium-блокер как открытый, хотя платформенно он уже решён
    ранее (образ пересобран, `vibe-krutko77` пересоздан)
  - Если Chromium-зависимые скиллы понадобятся другим ученикам — их
    контейнеры тоже нужно пересоздать (`docker rm -f vibe-<user>`), это не
    сделано автоматически ни для кого, кроме `vibe-krutko77`
  - Агент в `es-trans_repairs` ещё не вызывал реальный `POST /deploy` через
    `deploy-service` — сервис готов и протестирован, но живой редеплой этого
    конкретного проекта через него ещё не выполнялся (тянется с 2026-07-24)
  - `a1track` тоже уже в реестре deploy-service, но ни разу реально не
    деплоился через него (`es-trans-standard` — уже да, см. тикет 22.09)
  - `es-trans-test`/`test-1.es-trans.ru` (папка `es-trans-test-OM`) осознанно
    НЕ зарегистрирован в deploy-service — решение отложено на пользователя
  - `scripts/create-admin.sh` рассинхронизирован с актуальным
    `students.js → dockerCreate()` (старые лимиты ресурсов, устаревший путь
    шаблонов, нет общих volume'ов/SSH-маунтов) — воркэраунд задокументирован
    в `docs/expand_vibe-portal.md`, сам скрипт не исправлен (тянется с
    2026-07-22)
  - HTTPS на `201.51.4.183` включён и проверен curl'ом, но не проверен
    вживую в браузере под admin-сессией (тянется с 2026-07-22)
- **С чего начать:** `docs/last-session.md` → раздел «Что делать следующему»
