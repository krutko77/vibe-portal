# Состояние проекта

_Обновляется командой `/end` (или `/log`) в конце сессии/шага. Последнее обновление: 2026-07-24._

## Где мы сейчас

- **Новый компонент: `deploy-service`** (`172.30.0.1:8191`, `systemd/vibe-deploy.service`).
  Появился из инцидента: агент в личном (не курсовом) проекте admin'а
  `es-trans_repairs` попросил root на сервере `201.51.4.183` для деплоя.
  Разбор показал, что этот сервер — тот же хост, что держит весь
  vibe-portal, с уже живой production-инфраструктурой (5 Node/pm2-сайтов +
  1 PHP/php-fpm) для личных проектов admin'а из `/home/my_workspace`. Вместо
  root/sudo контейнеру — узкий сервис по образцу `anthropic-shim`: владельца
  резолвит по source-IP контейнера, домен ограничен `*.es-trans.ru`, сам
  выполняет `rsync`+`nginx`+`certbot`+`pm2` от root по запросу агента.
  Контракт для агента и полная архитектура — в `CLAUDE.md` § «Деплой на этот
  же хост…», решения и альтернативы — `docs/decisions.md` D-004/D-005.
- Реестр `/etc/vibe-deploy/registry.json` (вне git, в `/etc/`) заполнен для
  4 Node-проектов (`es-trans_repairs`, `es-trans_orders-and-transportation`,
  `es-trans_tests-results`, `es-trans-standard`) и 1 PHP-проекта (`a1track`,
  новый `type:"php"` в реестре — `rsync`+`chown www-data` вместо pm2, поля
  `src_subdir`/`exclude` для исходника не в корне проекта и прод-секретов/
  рантайм-данных, которые нельзя затирать). Сверка легаси-сайтов делалась по
  факту (`pm2 jlist`+`ss -ltnp`), не по именам — домены `test-1`/`test-2`
  оказались проксированы на процессы с «противоположными» на вид именами.
  `es-trans-test`/`test-1` (папка `es-trans-test-OM`) осознанно НЕ
  зарегистрирован — не запрашивалось.
- Попутно найден и исправлен регресс: `HOST_PUBLIC_IP` в
  `scripts/setup-iptables.sh` был захардкожен на устаревший адрес хоста
  (DHCP сменил IP на `201.51.4.183`) — анти-NAT-loopback DROP-правило
  (тот же защитный механизм, что чинили в D-003 два дня назад) молчал,
  0 совпадений. Теперь `HOST_PUBLIC_IPS` вычисляется динамически
  (`ip -4 -o addr show scope global`) при каждом запуске.
- Инструкция для агента в `es-trans_repairs` — файл
  `DEPLOY-INSTRUCTIONS-es-trans_repairs.txt` в корне репозитория (копия того
  же текста, что и в опубликованном артефакте с кнопками копирования).
- `docs/expand_vibe-portal.md` дополнен § 12: deploy-service как
  опциональный шаг для новой установки (если на сервере будут ещё и личные
  production-деплои admin'а) + урок про сверку легаси-сайтов по pid/порту.
- HOME разведён по ролям (admin=`/home/my_workspace`,
  ученик=`/home/student/workspace`) — см. CLAUDE.md, «HOME по роли».
  Правка `vibe-entrypoint.sh` (`HOME=$HOME` в `SetEnv` конфига sshd, чтобы
  десктопный VS Code по SSH тоже видел верный HOME) закоммичена этим снимком.
- Untracked `.bak-*` файлы (остатки вчерашней миграции) проверены построчно
  и удалены — все были точными пре-правочными копиями уже закоммиченных
  изменений, ничего не потеряно.
- Добавлена инфраструктура журналирования сессий: команды `/end`, `/log`,
  `/decision`, `/handoff` (`.claude/commands/`, источник — репозиторий
  ученика курса krutko77, содержимое проверено перед установкой) +
  `docs/state.md`, `changelog.md`, `last-session.md`, `handoff.md`,
  `decisions.md`, `scripts/snapshot.sh` (`git add -A && git commit`).
- Добавлен скилл `chat-history` (`.claude/skills/chat-history/`) — экспорт
  диалогов Claude Code этого проекта в `.claude/chat-history/*.md`
  (гитигнорится, личный рабочий лог).
- Изоляция iptables между контейнерами учеников **починена** (2026-07-22):
  `setup-iptables.sh` больше не хардкодит `br-vibe`, вычисляет реальное имя
  моста динамически из `docker network inspect vibe-net` — см.
  `docs/decisions.md` D-003. Проверено практически на `vibe-krutko77`.
- Написан раннбук деплоя с нуля — `docs/expand_vibe-portal.md`. Отдельный
  документ от `new-server-setup.md` (тот — проксмокс/железо чужого сервера,
  этот — конкретно установка vibe-portal). Покрывает: пакеты хоста, сборку
  `vibe-workspace:dev`, создание `vibe-net`, секреты (`.credentials.json`,
  `proxy.env`, `vibe-panel.env`), systemd-юниты, nginx+certbot, бутстрап
  первого admin'а, смоук-тест изоляции.
- Попутно найден и задокументирован (не исправлен) баг:
  `scripts/create-admin.sh` создаёт контейнер устаревшим способом — без
  общих volume'ов (`vibe-claude-skills`/`vibe-extensions`), без SSH-маунтов
  и персиста `.vscode-server`, со старыми лимитами памяти/CPU (1.5G/1.0
  вместо актуальных 3G/1.5) и устаревшим `TEMPLATES_DIR=/opt/dev-portal/templates`.
  Реальная логика — только в `students.js → dockerCreate()`, и она
  подхватывается лишь когда контейнера нет вообще (404), а не когда он
  просто устарел. Воркэраунд задокументирован в `docs/expand_vibe-portal.md`
  (`docker rm -f` после `create-admin.sh`, дать панели пересоздать
  контейнер актуальной функцией при первом старте).
- Включён HTTPS (самоподписанный сертификат) для доступа по голому IP
  `201.51.4.183` — убирает предупреждение "insecure context" у
  `/admin-code/`. Обнаружено и устранено: `/etc/nginx/sites-available/vibe`
  (IP-хост) вообще не был под git — отдельный, разошедшийся с репозиторием
  файл (в отличие от `nginx/vibe.kiselevgroup.com`, который под git, но
  для домена). Заведён в репозиторий как `nginx/201.51.4.183`, применён на
  сервере: `:80` → редирект на `:443`, сертификат в `/etc/nginx/ssl/`
  (вне git), проброс WS-заголовков сохранён (Баг 5 не проявился при
  smoke-тесте). Описано в `docs/expand_vibe-portal.md` (раздел 8b).

## Сейчас в работе

Активной незавершённой работы нет. `deploy-service` поднят, протестирован
smoke-тестом (health, 403 неизвестный контейнер, 400 path-traversal, полный
happy-path на одноразовом тестовом проекте) и закоммичен; реестр заполнен.
Агент внутри `es-trans_repairs` ещё НЕ вызывал реальный `POST /deploy` —
это следующее действие делает сам агент/пользователь, не эта сессия.

## Следующие шаги

1. Дождаться/выполнить первый реальный вызов `POST /deploy` из
   `es-trans_repairs` (инструкция — `DEPLOY-INSTRUCTIONS-es-trans_repairs.txt`
   в корне репо) и убедиться, что редеплой прошёл (`rsync`+`npm install`+
   `pm2 restart`), сайт не упал.
2. Если понадобится — так же прогнать через deploy-service `es-trans-standard`
   и `a1track` (уже в реестре, просто ещё не было реального вызова с их
   стороны) и проверить на живых доменах `test-2.es-trans.ru`/
   `a1track.es-trans.ru`.
3. `es-trans-test`/`test-1.es-trans.ru` (папка `es-trans-test-OM`) — не
   зарегистрирован в deploy-service, спросить пользователя, нужно ли.
4. Решить: чинить ли `scripts/create-admin.sh` (синхронизировать с
   актуальным `dockerCreate()`), или оставить как есть с воркэраундом —
   см. `docs/expand_vibe-portal.md`.
5. Обкатать команды (`/log`, `/decision`, `/handoff`) в реальной работе.
6. Проверить `/admin-code/` под живой admin-сессией в браузере через
   `https://201.51.4.183/` — убедиться, что VS Code не отваливается (1006)
   после принятия предупреждения о самоподписанном сертификате.
