# app-pult24 — Пульт руководителя

KPI, воронка продаж, рейтинг менеджеров и аналитика сделок в реальном времени. Подключается к вашему Битрикс24.

**Категория:** CRM · Аналитика
**Оценка часов:** ~9 часов
**Платформа:** Битрикс24

## Что это

Дашборд для Битрикс24 — видите всё по отделу продаж в одном экране: KPI, воронка, рейтинг менеджеров, аналитика сделок. Подключается к вашему порталу за 1–2 рабочих дня.

## Что внутри шаблона

Это **демо-версия** приложения: рабочий UI + синтетические данные.
Бэкенда нет — фронтенд перехватывает все вызовы `/api/*` через `js/demo-shim.js`
и возвращает моки из `js/demo-data.js`. Открывается в браузере без сервера.

## Как открыть

1. На vibe-портале / dev-портале нажми «Создать из шаблона» → `app-pult24`.
2. В VS Code открой `index.html`.
3. Запусти через Live Server (расширение VS Code) или просто открой файл в браузере.

## Фичи (из исходного приложения)

- Сводка KPI: выручка, конверсия, средний чек, количество сделок
- Рейтинг менеджеров с динамикой по периодам
- Воронка продаж по стадиям CRM
- График сделок по дням/неделям/месяцам
- Реальные данные из вашего Битрикс24
- Кастомизация под ваши воронки и показатели

## Куда копать дальше

- `index.html` — структура страницы (вкладки/блоки)
- `js/app.js` — корень приложения, инициализация
- `js/demo-data.js` — генератор моков, можно править чтобы поиграться с цифрами
- `js/demo-shim.js` — перехват `fetch('/api/...')` для подмены ответов
- `css/` — стили (большинство в духе Битрикс24)

## Если хочешь сделать реальную интеграцию

Заменить `demo-shim.js` на бэкенд который ходит в Битрикс24 REST:

```
https://<portal>.bitrix24.ru/rest/<user_id>/<webhook_token>/
```

Подробности — на dev.kiselevgroup.com/wiki/ в разделе «Битрикс24 / REST API».

---

*Шаблон сгенерирован из showcase-демо `pult24`. Полное описание
приложения, цена и форма заказа — на apps.kiselevgroup.com/apps/pult24.*
