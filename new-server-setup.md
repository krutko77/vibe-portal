# Настройка сервера — handoff (ФИНАЛ, заказан)

> Задание для новой сессии Claude. Сервер **заказан**, едет с предустановленным
> Proxmox. Здесь вся вводная: железо, цель, диски, сеть, бэкапы, VM, план настройки.
> Читать сверху вниз. Раздел «Пошаговый план» (§10) — что делаем руками после доставки.

---

## 0. Что заказано (факт)

**Провайдер:** smartape.ru · тариф **SmartDedic Gold-3842NR**
**Дата-центр:** DataPro Moscow III (RU) — Россия (под 152-ФЗ, данные в РФ)

| Параметр | Значение |
|----------|----------|
| CPU | **2 × Intel Xeon Gold 6148** = 80 потоков (40 ядер), 3.7 ГГц турбо |
| RAM | **256 ГБ** DDR4 |
| Диски | **2 × 3840 ГБ NVMe** (одиночные, **БЕЗ зеркала** — осознанно) |
| IP | **5 × IPv4 + 1 × IPv6** |
| Канал | 1 Гбит/с порт, **трафик 100 Мбит/с** (важно для бэкапов, см. §6) |
| ОС | **Debian 12** |
| Установленное ПО | **Proxmox** (предустановлен) |

Доступ к Proxmox: `https://<IP-админ>:8006` (после доставки придут IP + root-пароль).

---

## 1. Цель и стратегия (ДВА этапа)

**Этап 1 (этот сервер, сейчас):** гипервизор Proxmox с изолированными VM:
- **1С** на Linux — **1 пользователь** (нагрузка лёгкая), отдельная VM.
- **planka.market** — мой сервис, отдельная VM.
- **dev-portal + vibe-portal** — мои существующие порталы (vibe крутит docker-контейнеры
  учеников внутри VM — это норма), отдельная VM.
- **Транскрайбер** — фоновая нейронка GigaAM v3, распознаёт видео-архив **на CPU**.
- Видео-записи встреч/звонков/обучений (~3 ТБ, +15–25 ГБ/мес) — хранятся локально.

**Этап 2 (потом, отдельный сервер):** «взрослая» LLM на **отдельном GPU-сервере**
(аренда, когда реально понадобится). На этап 1 GPU и EPYC НЕ нужны — см. §9.

**Почему так:** 1С на 1 пользователя не требует EPYC/топ-CPU; LLM серьёзная — это
GPU, а его выносим отдельно, чтобы не платить заранее и не рисковать production-1С.
Gold 6148 (80 потоков, 256 ГБ) — оптимум цена/возможности для этапа 1.

**Толерантность к простою:** пара часов простоя НЕ критична → зеркала дисков не
делаем (экономим место), ставка на бэкапы (§6).

---

## 2. Архитектура

```
Сервер smartape (Proxmox VE, Debian 12)
│
├─ Диск A: 3840 NVMe  → zpool "vmpool"  (горячее, VM)
│   ├─ vm-1c          (Linux + 1С-сервер + PostgreSQL)   IP-1
│   ├─ vm-planka      (planka.market)                     IP-2
│   ├─ vm-portals     (dev-portal + vibe-portal)          IP-3
│   └─ vm-transcriber (GigaAM v3, CPU, фон)               — (без публичного IP)
│
└─ Диск B: 3840 NVMe  → zpool "datapool"  (холодное, данные+бэкапы)
    ├─ видео-архив (~3 ТБ, растёт)        ← читает vm-transcriber
    └─ бэкапы VM (PBS, дедуп)             ← бэкап VM на ДРУГОМ диске (крест!)
            │
            ▼ инкремент наружу (restic/Borg)
    Домашний ПК (внешний контур бэкапа)
```

Proxmox-админка (web-UI :8006) — на IP-4, за firewall (только мой IP/VPN).

---

## 3. Диски — БЕЗ зеркал, крест-накрест (КЛЮЧЕВОЕ)

2× 3840 NVMe используются **как два независимых диска** (не зеркало). Каждый —
**отдельный однодисковый ZFS-пул** (ZFS, не ext4/LVM: даёт контроль целостности,
снапшоты, сжатие, `zfs send` для бэкапа на ПК; без зеркала не самолечится, но
порчу хотя бы видит).

| Диск | Пул | Содержит |
|------|-----|----------|
| A (3.84 ТБ NVMe) | `vmpool` | диски всех VM (1С, planka, порталы, транскрайбер) |
| B (3.84 ТБ NVMe) | `datapool` | видео-архив (~3 ТБ) **+** бэкапы VM (PBS, дедуп) |

**Правило крест-накрест (важно!):** бэкапы VM лежат на диске B, а сами VM — на
диске A. **Нельзя** держать VM и их бэкап на одном диске. Тогда:
- умер диск A (VM) → виртуалки встали, поднимаем из бэкапов на диске B → пара часов;
- умер диск B (видео+бэкапы) → VM **продолжают работать** без простоя, видео и
  бэкапы тянем с домашнего ПК;
- потерян весь сервер → всё восстанавливаем с домашнего ПК.

**Почему локально, а не S3:** доплата за бóльшие NVMe (2×3840) дешевле/сопоставима
с арендой S3 на 3 ТБ, но быстрее и проще (всё своё, в РФ). Видео на NVMe = ещё и
транскрибация читает его быстро.

**Рост:** понравится сервер → докупить диски и собрать зеркала на лету
(`zpool attach`), без простоя.

---

## 4. Сеть и 5 IPv4

Каждому сервису — свой публичный IP (bridged `vmbr0`). Reverse-proxy НЕ нужен
(раз IP хватает). vm-transcriber публичного IP не имеет (внутренняя сеть `vmbr1`/NAT).

| IP | Назначение |
|----|------------|
| IPv4 #1 | vm-1c |
| IPv4 #2 | vm-planka (planka.market) |
| IPv4 #3 | vm-portals (dev + vibe) |
| IPv4 #4 | админка Proxmox (web-UI :8006, **firewall — только мой IP/VPN**) |
| IPv4 #5 | запас (или отделить dev от vibe) |

**DNS:** свой DNS-сервер НЕ поднимаем (он требовал бы 2 NS на 2 IP). Используем
**управляемый DNS российского провайдера** (reg.ru / Selectel / Yandex Cloud DNS —
под РФ-юрисдикцию). Просто прописываем A-записи доменов на нужные IP. Отдельные IP
«под DNS» не нужны — все 5 уходят на сервисы.

---

## 5. Как управляем сервером

- **Основной инструмент — веб-интерфейс Proxmox** (`https://IP-4:8006`): создание/
  старт/стоп VM, снапшоты, бэкапы, мониторинг (CPU/RAM/диск), консоль VM (noVNC).
- Web-UI на IP-4, **жёстко за firewall** (только мой IP/VPN). Наружу :8006 не светим.
- **SSH к хосту** — по ключу (пароль off), ограничен по IP. Для ZFS/тяжёлых операций.
- Доступ к VM: 1С/сайты — по своим IP; админ-вход в VM — по SSH-ключу.

---

## 6. Бэкапы (локально крест + домашний ПК)

```
[vmpool: боевые VM]
   │ ночной бэкап (Proxmox Backup Server, дедуп+инкремент)
   ▼
[datapool: бэкапы VM]   ← на ДРУГОМ диске (крест), быстрый откат
   │ инкремент наружу (restic/Borg)
   ▼
[Домашний ПК]  ← единственная внешняя копия (зеркал нет → обязателен!)
```

**Локально (PBS):** Proxmox Backup Server, датастор = `datapool`. Ночные бэкапы всех
VM, дедуп+инкремент. Retention напр. 7 дней / 4 недели / 3 месяца.

**Внешний контур — домашний ПК (вместо S3):**
- Бесплатно, данные в моём физическом контроле (РФ, закон ок).
- **ОБЯЗАТЕЛЬНО включает видео** — на сервере видео в ОДНОЙ копии (диск B без
  зеркала), дом = его единственная вторая копия.
- **ПК сам тянет** с сервера (он за домашним NAT): restic/Borg по расписанию (cron),
  подключается по SSH, забирает дельту, шифрует на клиенте.
- ⚠️ **Канал сервера 100 Мбит/с** (~12.5 МБ/с) → первая заливка ~3 ТБ домой займёт
  ~2.5–3 суток. Дальше только дельта (видео +~0.3 ТБ/год, бэкапы VM — гигабайты) —
  быстро. Заложи время на первый бэкап.
- ПК должен быть **включён по расписанию** + ~4 ТБ свободного места.

**Дисциплина:**
- **1С/PostgreSQL** — бэкапить **часто** (БД меняется постоянно), при выключенной VM
  или через снапшот (консистентность). Это бизнес-данные — приоритет №1.
- Раз в месяц **тестово восстановить** одну VM с домашнего ПК (непроверенный бэкап =
  миф).
- ZFS `scrub` раз в неделю на обоих пулах (ловит тихую порчу).

---

## 7. Распределение ресурсов по VM (256 ГБ, 80 потоков)

RAM режем жёстко (сумма < 256 − ~16 хосту). vCPU с овербукингом (потоков 80 — вагон).
Запаса море — при желании можно фоном гонять и небольшую LLM на CPU.

| VM | vCPU | RAM | Диск (vmpool) | IP | Заметки |
|----|------|-----|---------------|----|---------|
| vm-1c | 8 | 32 ГБ | 100–150 ГБ | IP-1 | **Linux + 1С-сервер для Linux + PostgreSQL** (лучше **Postgres Pro для 1С**). **1 пользователь** — нагрузка лёгкая |
| vm-planka | 4 | 8 ГБ | 40 ГБ | IP-2 | planka.market |
| vm-portals | 8 | 32 ГБ | 100–150 ГБ | IP-3 | dev+vibe; vibe крутит docker-контейнеры учеников (вложенные контейнеры — норма) |
| vm-transcriber | 8–12 | 24–32 ГБ | 40 ГБ + чтение `datapool` | — | GigaAM v3, фоном жуёт видео → текст |
| **Proxmox host** | остаток | ~16 ГБ | — | IP-4 (admin) | резерв |

Сумма RAM ≈ 116 ГБ из 256 — огромный запас. (vm-mgmt отдельно не нужен — админка на
хосте через IP-4.)

---

## 8. Транскрибация GigaAM v3 (CPU, фон)

Источник — видео-архив на `datapool`. Скорость не нужна → фоновая пакетная обработка
**на CPU** (80 потоков — есть чем; GPU не требуется, GigaAM — небольшая модель ~240M).

- Репозиторий: https://github.com/salute-developers/GigaAM (MIT, русский ASR).
- В vm-transcriber: Python ≥3.10 + ffmpeg, `pip install -e .[torch]`.
- Модель `v3_e2e_rnnt` (текст сразу с пунктуацией/нормализацией).
- Демон/cron: сканирует архив, новые файлы → транскрипт (текст занимает копейки).
  ```python
  import gigaam
  model = gigaam.load_model("v3_e2e_rnnt")
  text = model.transcribe("/mnt/datapool/archive/<file>.mp4")  # ffmpeg сам сконвертит
  ```

---

## 9. Этап 2 — отдельный GPU-сервер под LLM (потом)

Когда LLM станет реальной задачей — **отдельный сервер с GPU** (сначала аренда):
- GPU решает в LLM, а не CPU. Для моделей до ~34B → **24 ГБ карта** (RTX 3090 /
  A5000 / 4090); 32 ГБ (5090) — запас; A100/H100 — только под честный 70B+.
- Аренда (гибко) → покупка, если пойдёт 24/7 вдолгую.
- Связь по сети: GPU-сервер отдаёт LLM-API, порталы/приложения ходят к нему.
- На GPU же резко ускорится и транскрибация, если перенести её туда.

Подобранные кандидаты на этап 2 (для справки): EKACOD — 2× E5-2696 v4 + RTX A5000,
256 ГБ, NVMe (~48 810 ₽); но под LLM лучше брать сервер со свежей картой по факту нужды.

---

## 10. Пошаговый план настройки (после доставки)

1. **Доступ.** Получить IP (5×IPv4) + root-пароль. Зайти `https://IP-4:8006`.
   Сменить пароль, включить SSH-ключ, выключить пароль-логин, firewall (UI/SSH —
   только мой IP/VPN), fail2ban, обновления.
2. **ZFS-пулы.** Proxmox уже на одном диске (дефолт). На втором диске создать пул
   `datapool`. Первый диск под VM — пул `vmpool` (или штатное ZFS-хранилище
   Proxmox). Датасеты: `datapool/archive`, `datapool/vm-backup`.
3. **Сеть.** `vmbr0` (bridged, 5 публичных IP на VM), `vmbr1` (внутренняя/NAT — для
   vm-transcriber). Вписать выданные провайдером IP/шлюз/маску.
4. **PBS.** Поставить Proxmox Backup Server, датастор → `datapool/vm-backup`,
   расписание ночных бэкапов, retention.
5. **Внешний бэкап (домашний ПК).** restic/Borg на ПК, pull по SSH, шифрование,
   расписание, retention. Залить видео + бэкапы VM (учесть 100 Мбит/с на первой
   заливке). **Проверить восстановление.**
6. **VM.** Создать по §7: vm-1c, vm-planka, vm-portals, vm-transcriber. Каждой —
   свой IP (кроме transcriber), ресурсы, диск на `vmpool`.
7. **1С на Linux.** В vm-1c: Debian/Ubuntu + сервер 1С для Linux + Postgres Pro для 1С.
8. **Перенос порталов.** dev+vibe → vm-portals (см. их CLAUDE.md; vibe запускает
   docker-контейнеры учеников внутри). planka.market — развернуть/перенести.
9. **DNS.** Управляемый DNS РФ-провайдера (reg.ru / Selectel / Yandex) → A-записи
   доменов на нужные IP, Let's Encrypt сертификаты.
10. **Видео + транскрайбер.** Положить архив в `datapool/archive`, поднять GigaAM v3
    в vm-transcriber, запустить фоновую обработку.
11. **ZFS scrub** в cron (еженедельно), мониторинг места/SMART/SMART-NVMe.

---

## 11. Открытые решения / что уточнить

- **DNS-провайдер:** reg.ru / Selectel / Yandex Cloud DNS — выбрать один.
- **Домашний ПК для бэкапа:** какой (ОС, свободное место ≥4 ТБ, всегда ли включён).
- **planka.market:** текущее состояние/где живёт, docker-compose?
- **Точные IP/шлюз/маска** от smartape — вписать в `vmbr0`.
- **Этап 2 (GPU):** какие модели LLM реально нужны (определит выбор GPU).

---

## 12. Шпаргалка команд

```bash
# --- ZFS: пул на втором диске + датасеты ---
zpool create datapool /dev/disk/by-id/<nvme-B>
zfs create datapool/archive
zfs create datapool/vm-backup
zpool scrub datapool                       # проверка целостности (в cron, еженедельно)
zpool attach vmpool <nvme-A> <nvme-new>    # позже: собрать зеркало без простоя

# --- Proxmox backup/restore ---
vzdump <vmid> --storage pbs --mode snapshot
qmrestore <backup-file> <vmid> && qm start <vmid>

# --- restic на домашний ПК (ПК тянет с сервера, шифрованно, инкремент) ---
# на ПК:
restic -r sftp:root@<server-ip>:/datapool/vm-backup init
restic -r sftp:root@<server-ip>:/datapool/vm-backup backup /local/cache
# (видео: rsync/restic архива datapool/archive → ПК; первая заливка ~3 сут на 100 Мбит/с)
restic forget --keep-daily 7 --keep-weekly 4 --prune

# --- GigaAM v3 (vm-transcriber, CPU) ---
git clone https://github.com/salute-developers/GigaAM.git && cd GigaAM
python -m venv .venv && source .venv/bin/activate
pip install -e .[torch]      # ffmpeg в PATH

# --- Proxmox UI ---
# https://IP-4:8006  (за firewall, только мой IP/VPN)
```

---

## Стоимость (ориентир)

- Сервер smartape SmartDedic Gold-3842NR (2× Gold 6148 / 256 ГБ / 2×3840 NVMe /
  5 IPv4) — **~23 160 ₽/мес** (хит продаж −22%); годовая оплата у smartape **−10%**.
- Видео + бэкапы — **локально на NVMe** (без S3), внешний контур — **домашний ПК**
  (бесплатно). Доплата за NVMe оказалась выгоднее аренды S3.
- Этап 2 (GPU-сервер) — отдельно, по факту нужды.

**Тактика:** взять помесячно, обкатать 1–2 месяца (1С, vibe-контейнеры,
транскрибацию, бэкап на ПК) → если smartape устраивает, перейти на год (−10%).
