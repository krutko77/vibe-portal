# Настройка нового выделенного сервера — handoff

> Этот файл — задание для новой сессии Claude. В нём вся вводная: железо, цель,
> архитектура виртуализации, сеть/IP, диски, бэкапы и пошаговый план.
> Читать сверху вниз. Раздел «Пошаговый план» — то, что делаем руками.

---

## 1. Цель

Один физический сервер (аренда у **NetAngels, дата-центр в РФ** — осознанно,
чтобы не нарушать законодательство РФ при хранении/обработке данных) превращаем в
**гипервизор Proxmox VE** и поднимаем на нём изолированные виртуальные машины:

- **1С** (вероятно + SQL) — отдельная VM, свой публичный IP.
- **planka.market** — мой сервис, отдельная VM, свой IP.
- **dev-portal + vibe-portal** — мои существующие порталы, отдельная VM, свой IP.
- **Админка/DNS/шлюз** — управление сервером и доменами, отдельный IP.
- **Транскрайбер** — фоновая локальная нейронка (GigaAM v3), без своего IP.

Почему виртуализация: изоляция сервисов (упал/взломали один — остальные живы),
жёсткое деление RAM, удобный переезд на другое железо в будущем (см. §9),
встроенные бэкапы и снапшоты.

**Толерантность к простою:** полдня даунтайма не критично. Поэтому ставку делаем
на **сохранность данных (бэкапы)**, а не на аптайм (полные зеркала пока не берём).

---

## 2. Железо

| Компонент | Что |
|-----------|-----|
| CPU | **2 × Intel Xeon E5-2687W v4** = 24 ядра / 48 потоков, 3.0–3.5 ГГц |
| RAM | **128 ГБ** (8 × 16 ГБ DDR4-2400) |
| Диски | **1 × NVMe 1600 ГБ** + **1 × SAS 10000 ГБ** (одиночные, без зеркал — пока) |
| IP | **4 публичных IP** (распределение — §4) |
| Хостер / ДЦ | **NetAngels (netangels.ru), дата-центр в РФ** — данные в российской юрисдикции |

Сильные частоты Xeon E5-2687W v4 (3.0 ГГц база) — хорошо для 1С (она любит
быстрое одно ядро). 24 ядра / 128 ГБ — с большим запасом под все VM + транскрибацию.

---

## 3. Целевая архитектура

```
Физический сервер ── Proxmox VE (ставится на Debian 12 или штатным ISO)
│
├─ NVMe 1600 (zpool "fast")  ── горячее, полная скорость
│   ├─ VM  vm-1c          (1С + SQL)
│   ├─ VM  vm-planka       (planka.market)
│   ├─ VM  vm-portals      (dev-portal + vibe-portal)
│   ├─ VM  vm-mgmt         (reverse-proxy / админ-доступ / DNS-управление)
│   └─ VM  vm-transcriber  (GigaAM v3, фоновая транскрибация)
│
└─ SAS 10000 (zpool "tank") ── холодное, медленное, ёмкое
    ├─ dataset archive    (~2.4 ТБ) видео-записи встреч/звонков/обучений
    ├─ dataset vm-backup  (~1.6 ТБ) локальные бэкапы VM с NVMe (PBS)
    └─ dataset backups    (~5.0 ТБ) прочие бэкапы / сухие данные
            │
            ▼ инкремент наружу (ОБЯЗАТЕЛЬНО, см. §6)
    Внешнее хранилище в другом ДЦ
```

---

## 4. Сеть и IP (4 публичных адреса)

Каждой «выставленной наружу» VM — свой публичный IP через bridged-сеть Proxmox
(`vmbr0`). Раскладка:

| IP | VM / назначение | Что слушает |
|----|-----------------|-------------|
| IP-1 | **vm-1c** | 1С (RDP/веб-клиент 1С), доступ ограничить firewall'ом |
| IP-2 | **vm-planka** | planka.market (HTTP/HTTPS) |
| IP-3 | **vm-portals** | dev-portal + vibe-portal (HTTP/HTTPS) |
| IP-4 | **vm-mgmt** | админ-доступ к серверу (Proxmox UI за firewall), reverse-proxy, управление DNS/доменами |

### Как управляем сервером (наш план)

- **Основной инструмент — штатный веб-интерфейс Proxmox VE** (`https://IP-4:8006`):
  оттуда создаём/стартуем/стопаем VM, делаем снапшоты/бэкапы, смотрим нагрузку
  (CPU/RAM/диск), консоль каждой VM (noVNC) — всё из браузера, без отдельных панелей.
- Веб-UI вешаем на **IP-4** (vm-mgmt / админ-адрес) и **жёстко закрываем
  firewall'ом** — доступ только с моего IP / через VPN. Наружу `:8006` светить нельзя.
- **SSH к хосту** — по ключу (пароль выключен), тоже ограничен по IP. Для тяжёлых
  операций (ZFS, миграции, отладка) — командная строка по SSH.
- **Доступ к самим VM:** 1С — по своему IP (веб-клиент / сервер 1С), сайты — по своим IP;
  административный вход в VM — по SSH-ключу.
- (Опц.) на vm-mgmt можно повесить мониторинг (Netdata/Grafana) и reverse-proxy —
  единая точка наблюдения за сервером и сервисами.

Замечания:
- **vm-transcriber** публичного IP **не имеет** — она только читает видео с SAS и
  пишет текст, внешний доступ ей не нужен (внутренняя сеть `vmbr1` / NAT).
- Можно (опционально) собрать сайты за **одним reverse-proxy (nginx) на vm-mgmt**,
  но раз IP по одному на сервис — проще каждый сервис на своём IP напрямую.

### Про «управление доменами / A-записями / DNS»

IP-4 задуман в т.ч. под управление DNS моих доменов. Важный нюанс:
- **Авторитативный DNS на одном IP — хрупко** (регистраторы обычно требуют ≥2 NS
  на разных адресах/сетях). Поэтому **рекомендация**: авторитативный DNS держать у
  **российского провайдера/регистратора с управляемым DNS** (например, reg.ru,
  Selectel DNS, Yandex Cloud DNS — консистентно с РФ-юрисдикцией сервера), а
  IP-4/vm-mgmt использовать как **панель управления** (скрипты обновления A-записей
  через API провайдера, мониторинг, reverse-proxy, выдача Let's Encrypt-сертификатов).
- Если всё же нужен **свой** DNS-сервер — поставить **Technitium DNS** (удобный
  web-GUI) или **PowerDNS + PowerDNS-Admin** на vm-mgmt, но учесть требование 2×NS
  (второй NS — у регистратора/стороннего secondary).

> РЕШИТЬ с пользователем: Cloudflare (рекомендую) vs свой DNS-сервер. До решения —
> по умолчанию Cloudflare + vm-mgmt как control-plane.

---

## 5. Диски и ZFS

**Принцип:** NVMe = горячее (VM), SAS = холодное (архив + бэкапы). Полных зеркал
сейчас НЕТ (осознанно, ради экономии — простой допустим). Redundancy внутри
сервера не закладываем, поэтому **внешний бэкап обязателен** (§6).

### zpool `fast` (NVMe 1600, одиночный диск)
- Сюда ставится Proxmox и диски всех VM.
- ~1.4 ТБ usable. Сейчас данных ~600 ГБ — запас есть.

### zpool `tank` (SAS 10000, одиночный диск)
- **Не** делать жёсткие разделы. Один пул + **3 датасета с квотами** (границы
  гибкие, перекинуть место — одна команда):
  - `tank/archive`   quota ~2.4T — видео-записи (сейчас ~2 ТБ, рост 15–25 ГБ/мес)
  - `tank/vm-backup` quota ~1.6T — локальные бэкапы VM (PBS, дедуп)
  - `tank/backups`   quota ~5.0T — прочие бэкапы / сухие данные
- Итого ~9 ТБ ≈ usable одного 10 ТБ диска (впритык — поэтому квоты, не разделы).
- На самом ценном (`archive`) можно `copies=2` — две копии блоков на этом же диске
  (защита от тихой порчи/бэд-блоков; стоит двойного места; от смерти диска НЕ
  спасает). Решить по месту.

### Важно про NVMe→SAS
Это **бэкап по расписанию (PBS/restic)**, а **НЕ ZFS-mirror**. ZFS-mirror из
NVMe+SAS просадил бы запись до скорости SAS и убил смысл NVMe. Цель —
«умер NVMe → восстановились с SAS за полдня» — достигается именно бэкапом.

### Путь роста
Понравится сервер → **докупить второй NVMe и второй SAS** и превратить одиночные
диски в зеркала **на лету** (`zpool attach`, без простоя). Начать без зеркал и
дорастить — нормальная стратегия.

---

## 6. Бэкапы (правило 3-2-1)

Три рубежа. RAID ≠ бэкап: RAID спасает от смерти диска, бэкап — от удаления,
порчи, шифровальщика, краха/изъятия ДЦ.

```
[NVMe: боевые VM]
   │ ночной бэкап (Proxmox Backup Server, дедуп + инкремент)
   ▼
[SAS: tank/vm-backup]  ← быстрый локальный откат VM
   │ инкремент наружу (только дельта)
   ▼
[Внешнее хранилище в ДРУГОМ ДЦ]  ← единственная сетка, раз зеркал нет
```

**Локально:** Proxmox Backup Server (PBS) — инкрементально + дедупликация: после
первой полной копии гонит только изменённые блоки (ежедневный бэкап — гигабайты,
не терабайты). Ставим PBS на этот же сервер (датастор = `tank/vm-backup`).

**Внешне (ОБЯЗАТЕЛЬНО — нет локального зеркала):**
- Раз сервер в РФ по юридическим соображениям — **внешний бэкап тоже держим в РФ**
  (другой ДЦ / другой российский провайдер, чтобы копия не уезжала за границу):
  - **Selectel / VK Cloud / Yandex Object Storage** (S3-совместимое) + restic/rclone
    — платишь за ГБ, удобно под ~3–5 ТБ.
  - дешёвый **storage-VPS у российского хостера** (большой HDD) + второй PBS
    (PBS sync — дедуп и инкремент из коробки).
  - второй сервер/хранилище у **NetAngels в другом ДЦ** (если есть) — простой PBS-sync.
- Шифровать на клиенте (restic/Borg/PBS умеют) — данные 1С/видео лежат на чужом
  хранилище.
- **Видео-архив особенно:** сейчас у него ОДНА копия (на SAS). Нужна вторая
  (offsite). Первая заливка ~2–3 ТБ, дальше +0.3 ТБ/год.

**Дисциплина:**
- 1С/SQL бэкапить при выключенной VM или через снапшот (консистентность БД).
- Раз в месяц **тестово восстановить** одну VM из внешнего бэкапа (непроверенный
  бэкап = несуществующий).
- ZFS `scrub` раз в неделю (ловит тихую порчу).

---

## 7. Распределение ресурсов по VM (ориентир)

24 ядра / 48 потоков, 128 ГБ RAM. vCPU можно назначать с овербукингом (сумма >
физических потоков ОК — простаивающие отдают занятым). RAM — режем жёстко, сумму
держим ниже 128 минус ~8–10 ГБ хосту.

| VM | vCPU | RAM | Диск (NVMe) | IP | Заметки |
|----|------|-----|-------------|----|---------|
| vm-1c | 8 | 32 ГБ | 150–250 ГБ | IP-1 | **1С на Linux** (сервер 1С для Linux + PostgreSQL) — самая прожорливая, быстрый диск, при нужде CPU-pinning |
| vm-planka | 4 | 8 ГБ | 40 ГБ | IP-2 | planka.market |
| vm-portals | 6 | 16 ГБ | 80–120 ГБ | IP-3 | dev+vibe; vibe крутит docker-контейнеры учеников внутри VM (вложенные контейнеры — норма) |
| vm-mgmt | 2 | 4 ГБ | 20 ГБ | IP-4 | reverse-proxy, админ-доступ, DNS-control-plane |
| vm-transcriber | 6–8 | 16 ГБ | 40 ГБ + чтение SAS | — | GigaAM v3, фоном жуёт видео → текст |
| **Хост Proxmox** | остаток | ~8–10 ГБ | — | — | резерв |

Сумма RAM ≈ 84 + хост ≈ под 128 с запасом. CPU с овербукингом — спокойно.

- **1С чувствительна к производительности** — если будет «дёргаться» от соседей,
  включить **CPU pinning** (прибить конкретные физические ядра к vm-1c).
- **Транскрайбер** считает на CPU (24 ядра — есть чем). Скорость не важна — пусть
  фоном постоянно обрабатывает новые записи с `tank/archive`, текст складывает рядом.

---

## 8. Локальная нейронка для транскрибации (GigaAM v3)

Источник данных — видео-архив на SAS (`tank/archive`): записи встреч, звонков
менеджеров, внутренних обучений (~2.5→3 ТБ, +15–25 ГБ/мес). Скорость не нужна —
фоновая пакетная обработка.

- Модель: **GigaAM v3** (Сбер, open-source MIT, русский ASR, обходит Whisper-large-v3).
  Репозиторий: https://github.com/salute-developers/GigaAM
- Ставится в vm-transcriber: Python ≥3.10 + ffmpeg, `pip install -e .[torch]`.
- Рекомендуемая модель: `v3_e2e_rnnt` (текст сразу с пунктуацией и нормализацией).
- Считает на CPU (GPU нет — не страшно, задача фоновая). Можно демоном/cron:
  сканирует `tank/archive`, новые файлы → транскрипт (текст занимает копейки места).
- Пример:
  ```python
  import gigaam
  model = gigaam.load_model("v3_e2e_rnnt")
  text = model.transcribe("/mnt/archive/<file>.mp4")  # ffmpeg сам сконвертит
  ```

> Дальше (отдельная задача): организовать пайплайн «новая запись → транскрипт →
> (опц.) суммаризация/поиск». Пока цель — поднять фоновую транскрибацию.

---

## 9. Развитие в рамках этого ДЦ (переезда на другой сервер не планируем)

Полного переезда на другое железо/в другой ДЦ **не закладываем** — будем при
необходимости **улучшать в рамках NetAngels** (тот же ДЦ). Виртуализация всё равно
полезна: изоляция, бэкапы, снапшоты, гибкое деление ресурсов.

Пути роста без переезда:
- **Диски:** докупить второй NVMe и второй SAS → превратить одиночные диски в
  **ZFS-зеркала на лету** (`zpool attach`, без простоя) — поднять надёжность.
  Либо просто добавить диск под рост объёма (расширить пул/квоты).
- **RAM/CPU:** апгрейд тарифа/планок у NetAngels — VM переедут на расширенные
  ресурсы простым изменением лимитов в Proxmox.
- **VM:** добавлять новые сервисы — отдельные VM на том же гипервизоре.
- (Если когда-нибудь понадобится) точечный перенос VM на резервный сервер того же
  ДЦ — через бэкап PBS (`vzdump`/restore) или live-миграцию в Proxmox-кластере;
  но это не основной сценарий.

---

## 10. Пошаговый план настройки (то, что делаем)

1. **ОС/гипервизор.** Поставить **Debian 12 → Proxmox VE** (или штатный Proxmox ISO).
   - Корень системы — на NVMe (zpool `fast`). Debian → добавить репозиторий Proxmox
     → `apt install proxmox-ve` → reboot → web-UI `https://IP-4:8006`.
2. **ZFS пулы.**
   - `fast` уже под системой (NVMe).
   - `tank` на SAS: `zpool create tank /dev/<sas>` → датасеты `archive` / `vm-backup`
     / `backups` с квотами (§5).
3. **Сеть.** Настроить `vmbr0` (bridged, публичные IP-1…IP-4 на VM), `vmbr1`
   (внутренняя, NAT — для vm-transcriber). Firewall: Proxmox UI только с моего IP/VPN.
4. **Базовая безопасность хоста.** SSH по ключу (пароль off), fail2ban, обновления,
   firewall (только нужные порты на каждый IP).
5. **PBS.** Поставить Proxmox Backup Server, датастор → `tank/vm-backup`, расписание
   ночных бэкапов всех VM, retention (напр. 7 дней / 4 недели / 3 месяца).
6. **Внешний бэкап (в РФ).** Выбрать российское хранилище (Selectel / VK / Yandex
   Object Storage или storage-VPS у РФ-хостера / другой ДЦ NetAngels), настроить
   restic/Borg/PBS-sync, шифрование, расписание, retention. Залить видео-архив +
   VM-бэкапы. **Проверить восстановление.**
7. **VM.** Создать по §7: vm-1c, vm-planka, vm-portals, vm-mgmt, vm-transcriber.
   Каждой — свой IP (кроме transcriber), ресурсы, диск.
8. **Перенос существующего.** dev-portal + vibe-portal — мигрировать на vm-portals
   (см. их собственные CLAUDE.md; vibe запускает docker-контейнеры учеников внутри).
   1С и planka.market — развернуть/перенести.
9. **DNS.** Привязать домены к IP через управляемый DNS РФ-провайдера (reg.ru /
   Selectel / Yandex Cloud DNS), A-записи, Let's Encrypt сертификаты (на vm-mgmt
   reverse-proxy или на каждой VM).
10. **Транскрайбер.** Поднять GigaAM v3 на vm-transcriber, подключить `tank/archive`,
    запустить фоновую обработку.
11. **ZFS scrub** в cron (еженедельно), мониторинг места/SMART.

---

## 11. Открытые решения (уточнить с пользователем)

- **DNS:** управляемый DNS РФ-провайдера (reg.ru / Selectel / Yandex — рекомендую,
  консистентно с РФ-юрисдикцией) vs свой DNS-сервер на vm-mgmt (нужен 2-й NS).
- **Внешний бэкап:** какое РФ-хранилище (Selectel / VK / Yandex Object Storage
  vs storage-VPS+PBS vs другой ДЦ NetAngels).
- ~~**1С:** Windows или Linux?~~ → **РЕШЕНО: 1С на Linux.** vm-1c = Linux
  (Debian/Ubuntu) + **сервер 1С:Предприятие для Linux** + **PostgreSQL**
  (рекомендуется сборка **Postgres Pro для 1С** или патченный postgrespro —
  ванильный PostgreSQL с 1С работает хуже). Лицензии 1С (серверная + клиентские)
  — отдельно, гипервизор их не решает. ОЗУ под PostgreSQL не жалеть.
- **planka.market:** docker-compose? текущее состояние/где живёт сейчас?
- **Точные IP-адреса** и их шлюз/маска (выдаёт хостер) — вписать в конфиг `vmbr0`.

---

## 12. Шпаргалка команд

```bash
# --- ZFS ---
zpool create tank /dev/disk/by-id/<sas>            # пул на SAS
zfs create -o quota=2.4T tank/archive
zfs create -o quota=1.6T tank/vm-backup
zfs create -o quota=5T   tank/backups
zfs set copies=2 tank/archive                       # опц. защита от bit-rot
zpool scrub tank                                    # проверка целостности (в cron)
zpool attach fast <nvme1> <nvme2>                   # позже: превратить в зеркало

# --- Proxmox VM backup/restore (переезд/бэкап) ---
vzdump 100 --storage pbs --mode snapshot            # 100 = ID VM
qmrestore <backup-file> 100 && qm start 100

# --- restic наружу (шифрованно, инкремент) ---
restic -r sftp:user@storagebox:/backups init
restic -r sftp:user@storagebox:/backups backup /tank/archive /tank/vm-backup
restic forget --keep-daily 7 --keep-weekly 4 --prune

# --- GigaAM v3 (vm-transcriber) ---
git clone https://github.com/salute-developers/GigaAM.git && cd GigaAM
python -m venv .venv && source .venv/bin/activate
pip install -e .[torch]        # ffmpeg должен быть в PATH

# --- Proxmox UI ---
# https://IP-4:8006  (за firewall, только мой IP/VPN)
```

---

## Сводка по стоимости (диски, к diskless-базе сервера)

- NVMe 1600 (1 599 ₽) + SAS 10000 (3 798 ₽) = **5 397 ₽/мес** за диски.
- Внешний бэкап: **~1 100–2 500 ₽/мес** (зависит от провайдера).
- Без локальных зеркал (осознанно) — компенсируется обязательным внешним бэкапом.
